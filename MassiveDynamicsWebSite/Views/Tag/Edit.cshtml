@model SocialGameWebSite.Models.Tag

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset class="form-horizontal">
        <legend>Tag</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="control-group">
            @Html.LabelFor(model => model.nome, new { @class = "control-label" })
            <div class="controls">
                @Html.EditorFor(model => model.nome)
                @Html.ValidationMessageFor(model => model.nome, null, new { @class = "help-inline" })
            </div>
        </div>

        <div class="control-group">
            @Html.LabelFor(model => model.tipo, new { @class = "control-label" })
            <div class="controls">
                @Html.EditorFor(model => model.tipo)
                @Html.ValidationMessageFor(model => model.tipo, null, new { @class = "help-inline" })
            </div>
        </div>

        <div class="form-actions no-color">
            <input type="submit" value="Save" class="btn" />
        </div>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

