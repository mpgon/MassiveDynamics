@model SocialGameWebSite.Models.EstadoRelacao

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>


@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset class="form-horizontal">
        <legend>EstadoRelacao</legend>

        @Html.HiddenFor(model => model.ID)

        <div class="control-group">
            @Html.LabelFor(model => model.jogoID, "jogoID", new { @class = "control-label" })
            <div class="controls">
                @Html.DropDownList("jogoID", String.Empty)
                @Html.ValidationMessageFor(model => model.jogoID, null, new { @class = "help-inline" })
            </div>
        </div>

        <div class="control-group">
            @Html.LabelFor(model => model.estado, new { @class = "control-label" })
            <div class="controls">
                @Html.EditorFor(model => model.estado)
                @Html.ValidationMessageFor(model => model.estado, null, new { @class = "help-inline" })
            </div>
        </div>

        <div class="form-actions no-color">
            <input type="submit" value="Save" class="btn" />
        </div>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

